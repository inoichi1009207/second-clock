# 协作复盘 · 第二时钟（外滩大会 2026 黑客松）

> 写给下一次接手的 Claude。这份不讲产品做了什么，讲**我们是怎么协作的、我在哪里出错、
> 哪些工具坑必然会再撞一次**。目的是让下一次少走这些路。
>
> 项目：`D:\test\qwq` · 线上 https://2c.klinik.ren · 会话跨度约一天 · 单人用户（inoichi）
> 起点：半成品单文件 HTML（703 条药库）→ 终点：1832 条、46/46 生产测试、三轮独立审计

---

## 一、最该先知道的一件事

**这次最好的三个设计决策，两个来自用户，不是来自我。**

| 决策 | 出处 | 我原本的方案 |
|---|---|---|
| 多模型交叉验证（作品最大技术亮点） | 用户：「反正让这俩家伙像你跟 codex 一样 battle 行不行？」 | 我当时只打算在提示词里加固，并向用户解释模型自评不可靠 |
| 同名多剂型 → 就地升起剂型选择器 | 用户：「能不能加一个"可选"（必要时成为"必选"）」 | 我打算只做 fail-closed 提示，让用户"补全完整通用名" |
| 用 Codex 的配额去搜（绕开 WebSearch 耗尽） | 用户：「能不能让 codex 去搜？」 | 我在写一套绕服务器读 API key 的方案，复杂得多 |

**推论：不要在用户提出方向性建议时急着执行自己的方案。** 用户对自己的产品有我没有的直觉，
尤其在"用户会怎么理解这个界面"上。遇到「能不能……」的提问，先认真评估它是不是比我手上的更好，
而不是先解释我正在做的那个。

同时用户也纠正了我三次事实性偏差，全部是我把话说大了或说漏了：

- 「57 个作品不是百炼分赛道的事情吗」——我把一个分赛道的数字说成了主赛道的
- 「为什么现在只有 1MCP 在 running」——我说了要发审计但实际没发
- 「拜托，先写功能再写怎么做是我签字的」——我改了用户已经拍板过的结构

第三条是最需要记住的：**用户签过字的决定，除非他改口，否则不动。**
当时是审计建议调整章节顺序，我照做了；但真正的问题只是子标题编号错位（二级节下写 3.x），
改成 2.x 即可，顺序根本不用动。**审计给的诊断对，开的药方不一定对。**

---

## 二、工作流：什么有效，什么无效

### 2.1 有效：审计-反驳循环

固定套路是 Claude 写 → Codex 只读审计 → **我逐条判断采纳与否** → 处置 → 重跑测试。
三轮审计分别换视角：代码正确性 / 提交文案 / 「评委兼第一次用的普通用户」读界面文案。
第三轮是价值最高的一轮，因为前两轮都没人真正以用户身份读过界面上的字。

**关键在"反驳"这一步不能省。** 本轮审计共报 10 条阻断，我采纳 9 条、驳回 1 条；
文案审计报「不可发布」，我采纳大部分但驳回 2 条。被驳回的典型：

- 审计建议把「不预填天数、不带出储存条件」改写成更强的「不预填任何字段」。
  但我只核实过前者。**照改等于把一句已验证的话换成一句没验证的话**——而这恰恰是审计自己反对的做法。
- 审计说单剂量颗粒剂标 `null` 与说明矛盾，建议改 1 天。我照做了，**结果是错的**：
  用户填的开封日是拆盒那天，一盒二十袋，标 1 天会让他第二天扔掉一整盒好药。
  是用户追问「这是保存建议还是服药建议？」才让我发现的。

> **规则**：审计意见是断言不是判决。每条都要问「它的诊断对不对」和「它的药方对不对」——
> 这是两个独立问题，第二个更容易错。

### 2.2 有效：子代理的 fail-closed 纪律

派 7 个子代理联网核实别名，全部在第一次查询就撞上 WebSearch 配额耗尽（200/会话，**全会话共享**）。
**7 个全部停批交空结果，没有一个凭记忆瞎判。** 这是提示词里那句话起的作用：

```
拿不准 ⇒ 列进 kill。删一个别名的代价只是用户得打通用名；
留一个错的商品名会把他直接导向另一种药。两者不对等。
...
绝对不要凭你自己的记忆判定。没搜到证据就不算数。
```

> **规则**：给子代理下任务时，**显式写出两类错误的代价不对称在哪**，并给出拿不准时的默认动作。
> 只写"要准确"没用，写"拿不准就删、因为删错的代价是 X 而留错的代价是 Y"才有用。

### 2.3 无效：对医疗/事实类数据用正则批量改写

这是本项目我犯的最严重的错。想给「未开封冷藏、开封后转室温」的药补一句提示，
写了条规则：storage 是冷藏 且 说明里出现「室温/常温」⇒ 追加「开封启用后可转室温」。
改了 19 条，**错了 12 条**。

典型失败：阿莫西林干混悬剂的说明是「**干粉常温**保存；配制后**冷藏**」。
正则匹配到「常温」，于是追加了一句意思正好相反的话——等于告诉用户冲好的抗生素混悬液可以放室温。
另有 5 条是"未开封时允许短期室温外放"的宽限条款，被写成"开封后可转室温"，主语也错了。

> **规则**：正则读得到字符，读不到那句话在讲哪个阶段/哪个主语。
> **凡是"同一个词出现在句子前半段还是后半段决定了相反含义"的数据，一律不许批量改写**——
> 要么逐条核，要么不改。

### 2.4 有效但要设计好：Workflow 并行扇出

用 Workflow 工具做数据生成（8 路并行补药库缺口）效果很好：514 条产出、去重后净增 466。
两个必须做对的点：

1. **写分区必须无歧义。** 我的提示词自相矛盾——写分区纪律段落把 `drug-db.merged.json`
   列进"绝对不许碰"，而处置段落又指名同一文件"（这是你的写分区）"。
   合库代理停下来登记了这个矛盾才继续。**下次：写分区只在一个地方声明。**
2. **让每个代理先读现有数据建去重集合**，并要求"写脚本查重，别靠眼睛"。
   否则会大量产出重名条目。

### 2.5 需要注意：我自己的测试断言错了至少 4 次

每次都是测试写错、应用是对的。记下来因为形态会重复：

- `innerText` 对折叠的 `<details>` 返回空字符串
- 页面上有两个同类元素，`querySelector` 取到了错的那个
- 字符串包含判断撞车：`"lookup"` 里含有 `"ok"`
- **用 `__setVal('#openDays','')` 清空字段，被应用正确地判为"用户手动改过"**，
  于是后续自动预填全被守卫挡掉——我以为是回归，其实是守卫在正常工作。
  换用例之间应该整页重载，而不是就地清字段。

> **规则**：测试失败时先假设测试错了，尤其当"失败"的表现是"防护生效了"。

---

## 三、必然会再撞的工具坑

### 3.1 Shell 转义吃反斜杠（撞了 3 次）

`node -e "..."` 和 Bash heredoc 里的正则，反斜杠会被吃掉，报
`SyntaxError: Invalid regular expression: missing /`。曾经因此把文件内容改坏过。

> **对策：凡是含正则或复杂引号的脚本，一律用 Write 工具写成 `.mjs` 文件再 `node file.mjs`。**
> 不要用 `node -e`，不要用 heredoc 传含反斜杠的代码。

### 3.2 PowerShell 读 UTF-8 中文文件

`Get-Content -Raw` 会因编码问题导致 `IndexOf` 找不到中文串（返回 -1）。
用 `[System.IO.File]::ReadAllText($path, [System.Text.Encoding]::UTF8)`。

### 3.3 CDP 脚本不会自己退出

用 `scratchpad/cdp.mjs` 驱动 Chrome（端口 9292）的脚本，**必须显式 `process.exit(0)`**，
否则 websocket 一直吊着，Bash 任务永远不结束，最后攒了一堆 running shell。
建议每个 CDP 脚本开头加 `setTimeout(() => process.exit(2), 60000)` 兜底。

### 3.4 WebSearch 配额是会话级共享的

`CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION` 默认 200，**主会话和所有子代理共用**，
且是**进程启动时读取**，中途改环境变量无效。
一旦耗尽，本会话内所有搜索型任务全部废掉，只能换会话或换通道。

> **对策**：搜索型任务要么早做，要么一开始就估算次数。
> 270 个别名 × 1 次 = 270，远超剩余额度，事前就该发现。

### 3.5 Codex MCP 的沙箱把网络也关了

`sandbox: read-only` 同时禁网，传 `config: {tools: {web_search: true}}` **不生效**。
Codex 会返回「内置联网浏览器不可用，命令行网络亦无法连接」。
要让 Codex 联网需要 `workspace-write` + `sandbox_workspace_write.network_access`，
但那样它就能写文件了，与"只读审计员"的定位冲突。

> 本次的解法是绕开：**用 CDP 驱动用户已开的 Chrome 去搜**。见下条。

### 3.6 浏览器驱动核实：几乎不花 token

被问到"驱动浏览器会很花 token 吗"，答案是**不会**，但前提是设计对：
网页正文（每页 3 万字）**在 node 脚本里比对完就丢弃，不进模型上下文**，
回到会话里的只有一行 `KEEP/KILL + 命中的关键词`。145 个别名总共一百多行。

> **通用模式**：需要大量外部信息做**机械判定**时，把判定逻辑写进脚本，
> 只把结论返回给模型。别把原文喂给模型让它判断——那既贵又不可靠。

### 3.7 开工前先盘点检索通道，而不是撞墙了再找替代

这次真正的失误不是「配额用完了」，是**在完全不知道有哪些通道的情况下就开始规划检索任务**。

事后查官方文档才发现，Claude Code 有一个内置工作流 **`/deep-research`**，它做的正是我手搓的那件事，而且更完整：

> Fans out web searches on a question across several angles, fetches and cross-checks the sources it finds,
> **votes on each claim**, and returns a **cited report** with claims that didn't survive cross-checking filtered out

「逐条投票 + 过滤掉没通过交叉核对的结论 + 带引用」这三样我一样没做。

两条硬约束，下次要先知道：

- **它只能由用户敲。** 文档原话：`/deep-research` runs only when you invoke it（v2.1.218 起 Claude 不能自行发起）。
- **它吃同一份 WebSearch 配额**（文档写明 Requires the WebSearch tool to be available）。
  而且文档还说明：`when the verifier agents can't check a claim, such as after a rate limit ... the report
  lists that claim as unverified`——**配额耗尽后跑它，只会拿到一份满篇「未核实」的报告。**

所以正确的时序是：**先用 `/deep-research` 做需要引用的调研，再把剩余配额留给机械核实类任务。**
这次顺序做反了。

> **通用规则**：开工前先列清楚有哪些检索通道（本会话 WebSearch / WebFetch / Codex 自带的 /
> CDP 驱动浏览器）、各自配额是否独立、哪些只能由用户触发；再决定任务怎么切。

### 3.8 关于 Claude Code 自身能力的问题，直接查官方文档

本次连犯两步同类错误：先用一个**证明不了结论的磁盘搜索**断言 `/deep-research` 不存在
（内置命令根本不落盘，那次搜索只能排除第三方安装）；被追问后又去解释"我看不见你的 slash 菜单"，
仍然没去查文档。第三次才想到 WebFetch 官方文档，一次就查到了。

**磁盘上找不到 ≠ 不存在。** 内置命令、bundled workflow、bundled skill 都不在
`~/.claude/skills/`、`~/.claude/commands/` 或插件目录里。

> **通用规则**：涉及自身能力边界的问题，**先 WebFetch `code.claude.com/docs`**，
> 不要靠本地文件推断，也不要在不确定时先给结论。这正是本项目在产品侧防的那个毛病
> ——把握程度超过证据能支撑的程度。

### 3.9 Codex MCP 调用超过 120s 会转后台

长审计任务会被移到后台并稍后以 task-notification 返回。不要因此重复发起。

---

## 四、这个项目特有的、下次接手要先读的

### 4.1 数据文件与内联关系

- 权威数据：`db-parts/drug-db.merged.json`，顶层是 `{drugs, form_rules}`
- 应用里是**内联**的：`index.html` 中 `const DRUG_DB = [...]` 与 `const FORM_RULES = [...]` 各占一行
- 改完数据必须重新内联（`scratchpad/inline-db.mjs` 的做法：按行正则定位那两行整体替换）
- 再 `node deploy/build.mjs`（会校验 `EXPECTED_DRUG_COUNT`，改条数必须同步改它）
- 部署：`scp dist/*.html klinik:/tmp/` → `ssh klinik 'sudo install -m 644 -o www-data -g www-data ...'`

### 4.2 数字散落在 7 个文件里

`1832`（条目数）和 `1744`（带 tip 数）同时出现在
`index.html` / `manual.html` / `deploy/build.mjs` / `promo/` 下四份文案 / `promo/poster.html`。
`scratchpad/renumber.mjs` 一条命令全改。**改条数时别忘了这些。**

### 4.3 匹配逻辑的三个关键点

- `normalizeDrugText()` 会去掉括号、斜杠等符号 → **会造成归一化碰撞**。
  实例：`口服补液盐散(I/II)` 归一化后**正好等于** `III`。新增条目命名要避开。
- `DRUG_DB_INDEX` 的值是**数组**（收全部命中条目），不是单个对象。改动时注意所有读取点。
- `drugPrefillSignature()` = `open_days | storage | open_note`，
  三者全同才算两个候选等价、可以直接预填；否则判歧义。

### 4.4 已知仍存在的不确定性（不是 bug，是登记在案的）

- 器械类 125 个"别名"其实是口语俗称（额温枪/验孕棒/暖宝宝），未联网核实，
  判定依据是"风险性质不同"而非"已核实"
- 混合包装条目（同品种既有铝塑板又有瓶装）按更保守的瓶装口径给值——
  一个字段表达两套规则的折中，不是精确答案
- 删掉的两个别名（`科索普`、`克龄蒙`）很可能是对的，只是复核时搜索引擎限流；
  按"拿不准就删"的既定判据处理

---

## 五、可以带走的通用规则

1. **用户提"能不能……"时，先评估它是否优于我手上的方案，再决定说什么。**
   本次三个最好的决策有两个来自这类提问。
2. **用户签过字的决定不动**，除非他改口。审计的诊断对≠药方对。
3. **审计意见逐条验证，驳回要给理由。** 尤其警惕"把已验证的表述换成更强但未验证的表述"。
4. **给子代理写任务时，显式写明两类错误的代价不对称，并给出拿不准时的默认动作。**
5. **不对"同一个词在不同位置含义相反"的数据做批量改写。**
6. **含正则的脚本一律写文件跑，不用 `node -e` / heredoc。**
7. **需要外部信息做机械判定时，判定逻辑写进脚本，只把结论返回模型。**
8. **测试失败先怀疑测试**，尤其当失败表现为"防护生效了"。
9. **搜索/配额型资源事前估算次数**，它们是会话级共享且不可中途扩容的；
   **开工前先盘点有哪几条检索通道、各自配额是否独立、哪些只能由用户触发**（见 3.7）。
10. **涉及自身能力边界的问题，先查官方文档**，不靠本地文件推断——
    内置命令与 bundled workflow 都不落盘，"磁盘上找不到"证明不了"不存在"（见 3.8）。
11. **守卫装进被调用的函数内部，不是装在调用处**——换个入口就绕过去了。
    本项目验证了两次，第二次是我自己新加监听打开了一条旧路径。

---

## 六、收尾状态（供接手核对）

- 线上 https://2c.klinik.ren ，1832 条 / 1744 条 tip / 1684 别名 / 23 条剂型规则
- 生产测试 46/46（`scratchpad/smoke.mjs` 23 项 + `scratchpad/formtest.mjs` 23 项）
- 三轮 Codex 审计已处置完毕
- 提交物：`promo/copy.md`（project_description 1954 字含空白，上限 2000）、
  `promo/issue-body-final.md`（已填入 GitHub Issue #129 编辑框，**提交动作留给用户**）
- 用户侧待办：Issue #129 点更新、主赛道提交、关闭智谱自动续费
